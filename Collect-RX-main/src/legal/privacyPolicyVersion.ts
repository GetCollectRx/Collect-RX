/**
 * Bump this whenever the "Last updated" date in src/pages/LegalPrivacy.tsx changes —
 * it's what gets recorded against a practice's signup consent, so it must stay tied
 * to the exact policy text they agreed to.
 */
export const PRIVACY_POLICY_VERSION = '2026-04-25';
